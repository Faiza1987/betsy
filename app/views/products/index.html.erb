<h1><%= @title %></h1>

<div>
  <% @products.each do |product| %>
  <a href="<%= product_path(product) %>">
    <p><%= product.category_ids %></p>
    <h2><%= product.stock %> <%= product.name %></h2>
    <p>$ <%= ("%.2f" % product.price) %></p>
  </a>
  <% end %>
</div>