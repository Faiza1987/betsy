<%= form_with model: @order_item, url: product_orderitems_path(id: params[:product_id]) do |f| %>
  <%= f.hidden_field :product_id %>

  <%= f.label :quantity %>
  <%= f.text_field :quantity %>

  <%= f.submit "Add to Cart" %>
<% end %>
