<h1>Update order item</h1>

<%= form_with model: @order_item, class: "edit-orderitem" do |f| %>
  <%= f.hidden_field :product_id %>
  
  <%= @product_name %>

  <%= f.label :quantity %>
  <%= f.text_field :quantity %>

  <%= f.submit "Update Cart Item" %>
<% end %>
